#include <stdio.h>

int main (){
    int n; scanf("%d",&n);
    int arr[n+1];
    for (int i = 1; i <= n; i++)
    {
        scanf("%d",&arr[i]);
    }
    int q; scanf("%d",&q);
    
    for (int i = 1; i <= q; i++)
    {
        scanf
        
    }
    
    
    return 0;
}